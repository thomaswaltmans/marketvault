{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "uv sync --locked --no-dev && uv run --no-dev python manage.py collectstatic --noinput"
  },
  "deploy": {
    "preDeployCommand": ["uv run --no-dev python manage.py migrate --noinput"]
  }
}
